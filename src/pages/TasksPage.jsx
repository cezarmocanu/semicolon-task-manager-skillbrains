import React from "react";
import { Container, Typography } from "@mui/material";

function TasksPage() {
	return (
		<Container>
			<Typography variant="h1">TasksPage</Typography>
		</Container>
	);
}
export default TasksPage;
